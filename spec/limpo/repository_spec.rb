require "spec_helper"

describe Limpo::Repository do
  it "has a version number" do
    expect(Limpo::Repository::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
